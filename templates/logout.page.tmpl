{{template "base" .}}

{{define "content"}}

<div class="container">
    <div class="row">
        <div class="col">
            {{$uData := index .Data "userData"}}
            <h1>LoggedOut</h1>

        </div>
    </div>
</div>

{{end}}